# A cli program that helps create projects

## Types of projects

- cli
- react app with options to add linter, formatter, precommit, prepush hooks
- express app with options to add linter, formatter, precommit, prepush hooks
