#!/usr/bin/env node

// eslint-disable-next-line no-global-assign
require = require("esm")(module);
const args = require("minimist")(process.argv.slice(2));
module.exports = require("../src/cli.js").cli(args);
